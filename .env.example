# ============================================================
# SilverTalk 환경 변수 설정 (예시 파일)
# ============================================================
# 사용법: 이 파일을 .env로 복사하고 실제 값으로 수정하세요.
# cp .env.example .env
# 
# ⚠️ 주의: 실제 API 키나 비밀번호를 이 파일에 넣지 마세요!
# 이 파일은 Git에 commit되므로 예시 값만 포함합니다.
# ============================================================

# ============================================================
# 기본 설정
# ============================================================

# 환경 구분
ENVIRONMENT=development  # development, production

# 배포 모드 (LOCAL: docker-compose, CLOUD: EC2+RunPod)
DEPLOYMENT_MODE=LOCAL

# 로그 레벨
LOG_LEVEL=INFO

# GPU 설정 (RunPod 사용 시)
CUDA_VISIBLE_DEVICES=0

# ============================================================
# Redis 설정
# ============================================================

# 로컬 개발 환경 (Docker Compose 사용 시)
REDIS_URL=redis://redis:6379/0
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0

# 로컬 환경용 (LOCAL 모드)
LOCAL_REDIS_URL=redis://redis:6379/0

# Upstash Redis (RunPod/EC2용 - CLOUD 모드)
# https://upstash.com에서 Redis 생성 후 TCP 탭의 URL 복사
# 형식: rediss://default:PASSWORD@instance-name.upstash.io:6379
UPSTASH_REDIS_URL=rediss://default:your_password_here@your-instance.upstash.io:6379

# Upstash Redis REST API (선택사항)
UPSTASH_REDIS_REST_URL=https://your-instance.upstash.io
UPSTASH_REDIS_REST_TOKEN=your_rest_token_here

# ============================================================
# 데이터베이스 설정
# ============================================================

# 로컬 개발 환경 (Docker Compose 사용 시)
DATABASE_URL=postgresql://silvertalk:silvertalk_dev_password@postgres:5432/silvertalk
LOCAL_DATABASE_URL=postgresql://silvertalk:silvertalk_dev_password@postgres:5432/silvertalk

# RDS PostgreSQL (Production DB - CLOUD 모드)
# 형식: postgresql://username:password@rds-endpoint:5432/dbname
PROD_DATABASE_URL=postgresql://your_user:your_password@your-rds-endpoint.rds.amazonaws.com:5432/postgres

# ============================================================
# API 키
# ============================================================

# Gemini API 키 (필수!)
# https://makersuite.google.com/app/apikey 에서 발급
GEMINI_API_KEY=your_gemini_api_key_here

# Replicate API 토큰 (이미지/영상 생성용)
# https://replicate.com/account/api-tokens 에서 발급
REPLICATE_API_TOKEN=your_replicate_api_token_here

# ============================================================
# AWS S3 설정
# ============================================================

# AWS 자격 증명
AWS_ACCESS_KEY_ID=your_aws_access_key_id_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key_here
AWS_REGION=ap-northeast-2
S3_BUCKET_NAME=your-bucket-name-here
AWS_S3_BUCKET_NAME=your-bucket-name-here

# 저장소 백엔드 (LOCAL: 로컬 파일, S3: AWS S3)
STORAGE_BACKEND=LOCAL

# ============================================================
# 카카오 OAuth 설정
# ============================================================

# 카카오 개발자 콘솔에서 발급
# https://developers.kakao.com/console/app
KAKAO_CLIENT_ID=your_kakao_rest_api_key_here
KAKAO_REDIRECT_URI=http://localhost:8000/auth/kakao/callback

# ============================================================
# JWT 설정
# ============================================================

# JWT 시크릿 키
# 생성 방법: openssl rand -hex 32
# 프로덕션 환경에서는 반드시 새로 생성하여 사용!
JWT_SECRET_KEY=your_generated_secret_key_here_use_openssl_rand_hex_32

# ============================================================
# RunPod 설정 (선택사항)
# ============================================================

# RunPod API 설정
# https://runpod.io/console/user/settings
RUNPOD_POD_ID=your_pod_id_here
RUNPOD_API_KEY=your_runpod_api_key_here