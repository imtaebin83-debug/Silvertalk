# RunPod 웹 터미널에서 복사해서 한 줄씩 실행하세요
# (우클릭 붙여넣기 또는 Shift+Insert)

cd /workspace/Silvertalk/backend

# .env 파일 생성 시작
cat > .env << 'EOF'
DEPLOYMENT_MODE=CLOUD
CUDA_VISIBLE_DEVICES=0

# Redis (Upstash TCP 탭에서 복사한 값으로 교체)
# 예시: rediss://default:ARzRAA...@new-grizzly-7377.upstash.io:6379
UPSTASH_REDIS_URL=rediss://default:ARzRAAImcDI4ZDE2ZTZmZWJkOGY0OTVjOGM1NzE4N2ViN2FlNWM3OHAyNzM3Nw@new-grizzly-7377.upstash.io:6379

# Database (팀원에게 받은 실제 값으로 교체)
PROD_DATABASE_URL=postgresql://username:password@rds-endpoint:5432/silvertalk

# S3 (팀원에게 받은 실제 값으로 교체)
STORAGE_BACKEND=S3
AWS_ACCESS_KEY_ID=AKIAxxxxx
AWS_SECRET_ACCESS_KEY=xxxxxx
AWS_REGION=ap-northeast-1
S3_BUCKET_NAME=silvertalk-prod

# Gemini API (팀원에게 받은 실제 값으로 교체)
GEMINI_API_KEY=your_gemini_key
EOF

# .env 파일 확인
cat .env
