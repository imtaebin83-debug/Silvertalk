# EC2 전용 requirements (API 서버만)
# AI 모델 제외, FastAPI + DB + Redis만

# Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# HTTP Client (Kakao OAuth 등 외부 API 호출용)
httpx==0.24.1

# Async Task Queue (클라이언트만)
celery==5.3.4
redis==5.0.1

# Utilities
python-dotenv==1.0.0
requests==2.31.0
pydantic==2.5.3
pydantic-settings==2.1.0

# Database
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
alembic==1.13.1

# Image Processing (간단한 처리만)
Pillow==10.2.0

# AWS SDK (S3 업로드)
boto3==1.34.34

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
