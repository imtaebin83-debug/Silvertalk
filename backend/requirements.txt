# Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Async Task Queue
celery==5.3.4
redis==5.0.1
flower==2.0.1

# AI Models
faster-whisper==0.10.0
TTS==0.21.3  # Coqui XTTS v2 (안정적 버전)
google-generativeai==0.3.2

# Audio/Video Processing
# PyAV 명시적 설치 (TTS 의존성)
av==11.0.0
ffmpeg-python==0.2.0
pydub==0.25.1
soundfile==0.12.1
# librosa 제거: TTS와 numpy 버전 충돌 방지
# 우리 코드에서 librosa 미사용. TTS 내부 의존성만 있으면 됨.

# Image Processing
Pillow==10.2.0

# Utilities
python-dotenv==1.0.0
requests==2.31.0
pydantic==2.5.3
pydantic-settings==2.1.0

# Database
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
alembic==1.13.1

# AWS SDK (프로덕션 배포 시)
# boto3==1.34.34
